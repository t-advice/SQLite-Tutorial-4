<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SQLiteTutorial4.MainPage"
             Title="Image Gallery">

    <VerticalStackLayout Padding="12" Spacing="10">

        <HorizontalStackLayout Spacing="10" HorizontalOptions="Center">
            <Button Text="Capture Photo" Clicked="OnCapturePhotoClicked" CornerRadius="20" />
            <Button Text="Pick Photo" Clicked="OnPickPhotoClicked" CornerRadius="20" />
        </HorizontalStackLayout>

        <CollectionView x:Name="ImagesCollection"
                        ItemsLayout="VerticalGrid, 3"
                        SelectionMode="None"
                        HeightRequest="700">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="4" CornerRadius="60" HasShadow="False" BorderColor="#ddd" Margin="4">
                        <Image Source="{Binding FilePath}" Aspect="AspectFill" HeightRequest="120" WidthRequest="120">
                            <Image.Clip>
                                <EllipseGeometry Center="60,60" RadiusX="60" RadiusY="60" />
                            </Image.Clip>
                        </Image>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>

</ContentPage>

